#robot_radius: 0.075 # 机器人半径
footprint: [[0.46,0.2],[0.48,0],[0.46,-0.2],[-0.02, -0.2],[-0.02,0.2]]

footprint_padding: 0.10
footprint_inflation: 0.025 # 机器人膨胀层

transform_tolerance: 2.0

always_send_full_costmap: true  #true
transform_tolerance: 1.0
# publish_frequency: 0.0
obstacle_range: 3.0
# publish_frequency: 0.0
# obstacle_range: 50.0
obstacle_layer:
  enabled: true
  combination_method: 1
  track_unknown_space: true
  obstacle_range: 3 # 规避障碍的范围
  raytrace_range: 3 # 探测障碍的范围
  inflation_radius: 0.2
  inf_is_valid: true

  observation_sources: scan
  scan: {sensor_frame: laser, data_type: LaserScan, topic: /scan, 
  marking: true,clearing: true}  # 0.05

static_layer:
  enabled:              true
  map_topic:            "map"

